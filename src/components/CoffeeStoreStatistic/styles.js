import styled from 'styled-components';

const CoffeeStoreStatisticStyled = styled.div`
  margin-top: 50px;

  .your-stores-statistic {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 20px;
  }

  .ant-statistic {
    text-align: center;
  }
`;

export default CoffeeStoreStatisticStyled;
