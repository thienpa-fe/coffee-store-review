import styled from 'styled-components';

const ReactionsStatisticStyled = styled.div`
  margin-top: 50px;

  .card-top {
    display: flex;
    align-items: center;
  }

  .icon {
    margin-left: 10px;
  }

  .ant-typography {
    margin-top: 30px;
  }
`;

export default ReactionsStatisticStyled;
