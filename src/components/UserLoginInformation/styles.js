import styled from 'styled-components';

const UserLoginInformationStyled = styled.div`
  position: absolute;
  right: 0;
  top: -10px;

  .user-info-login {
    font-weight: bold;
  }
`;

export default UserLoginInformationStyled;
