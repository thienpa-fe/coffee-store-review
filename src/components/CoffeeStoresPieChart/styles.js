import styled from 'styled-components';

const CoffeeStoresPieChartStyled = styled.div`
  .recharts-default-legend {
    display: flex;
    flex-direction: column;
    text-align: unset !important;
  }
`;

export default CoffeeStoresPieChartStyled;
