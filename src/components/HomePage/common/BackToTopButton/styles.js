import styled from 'styled-components';

const BackToTopButtonStyled = styled.div`
  .back-to-top-btn {
    display: none;
    position: fixed;
    bottom: 20px;
    right: 30px;
    z-index: 99;
    cursor: pointer;
  }
`;

export default BackToTopButtonStyled;
